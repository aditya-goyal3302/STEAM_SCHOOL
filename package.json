{
  "dependencies": {
    "express-validator": "^7.0.1",
    "express": "^4.17.1",
    "body-parser": "^1.19.0",
    "mongoose": "^5.9.7",
    "bcryptjs": "^2.4.3",
    "jsonwebtoken": "^8.5.1",
    "config": "^3.3.1",
    "dotenv": "^8.2.0"
  },
  "scripts": {
    "start": "nodemon server/index.js",
    "build":"npm install --legacy-peer-deps && cd client && npm install --legacy-peer-deps && npm run build && cd .. && npm start"
    
  }
}
